enum Variants {
    primary = 'primary',
    secondary = 'secondary',
    defaultVariant = 'default-variant',
}

enum Sizes {
    small = 'small',
    default = 'default',
    rounded = 'rounded'
}

enum Types {
    button = 'button',
    submit = 'submit',
}

export {Variants, Sizes, Types};