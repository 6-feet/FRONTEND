import Button from '../components/UI/Button/Button';

const Home = (): JSX.Element => {
  return (
    <div>
      Hello, NextJS!
      <Button>click me</Button>
    </div>
  );
};

export default Home;
